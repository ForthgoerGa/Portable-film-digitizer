# Raspberry Pi scanner server deployment settings.
# Copy to software/src/.env on the Pi and edit for the active network.

PI_SERVER_HOST=0.0.0.0
PI_SERVER_PORT=5000

# URL reachable from the Pi for tile uploads back to the PC server.
PC_APP_URL=http://<pc-ip>:8000
